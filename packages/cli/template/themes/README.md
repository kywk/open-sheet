# Themes

Drop a `<id>.md` here to define a house style — palette, number formats, and
paste-ready components. Workbooks link to one with `meta.theme`.

Ask your agent for `/create-theme` to write one.
